# Example reference

Put supporting docs here — checklists, schemas, examples, edge cases.

Skill-local references travel with the skill when installed via `npx skills`.
Keep long or sensitive source material in `refs/` at repo root instead.
