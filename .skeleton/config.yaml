# yaml-language-server: $schema=../schemas/config.schema.json

scan:
  include:
    - "docs/**"
    - "README.md"
    - "AGENTS.md"
    - ".skeleton/registry.md"
    - "skeleton/**"
    - "editors/vscode/README.md"
  exclude:
    - "refs/**"
    - "todo.md"
    # Local skills-CLI install trees (not source) — avoid dual-SKILL dogfood fails
    - ".agents/**"
    - ".claude/**"
    - "editors/vscode/node_modules/**"
  banned: []
  retiredSkills: []

daysUntilStale: 180
