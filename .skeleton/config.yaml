# yaml-language-server: $schema=../schemas/config.schema.json

scan:
  include:
    - "docs/**"
    - "README.md"
    - ".skeleton/registry.md"
    - "skill/**"
  exclude:
    - "refs/**"
  banned: []
  retiredSkills: []

daysUntilStale: 180
