# yaml-language-server: $schema=../../../../schemas/config.schema.json

scan:
  include:
    - "docs/**"
    - ".claude/skills/**"
    - ".skeleton/registry.md"
  exclude: []
  banned: []
  retiredSkills: ["ci"]

daysUntilStale: 180
